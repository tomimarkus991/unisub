import { DefaultPageWrapper } from "components";

export const StatisticsPage = () => {
  return (
    <DefaultPageWrapper>
      <div className="flex flex-col w-full h-full cursor-pointer">stats</div>
    </DefaultPageWrapper>
  );
};
